--- _ag_manifest: AlifeGuard identity. Pure data, no logic. Read by _ag_init (the gate) and the validator (deps.manifest_structure).

MANIFEST = {
    name    = "AlifeGuard",
    version = "1.3.2-snapshot",
    xlibs   = "1.8.3",
}
